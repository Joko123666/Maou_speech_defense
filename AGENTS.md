# 프로젝트 컨텍스트 운영 지침

이 파일의 지침은 저장소 전체에 적용됩니다. 사람 또는 자동화된 작업 컨텍스트는 아래 절차로 프로젝트 정보를 공유합니다.

## 작업 시작 시 필수 절차

1. 사용자 요청을 확인한 직후 [`docs/SHARED_CONTEXT.md`](docs/SHARED_CONTEXT.md)를 먼저 읽습니다.
2. 작업과 관련된 실제 코드, 씬, 리소스, 테스트를 확인해 공유 문서의 정보가 아직 유효한지 검증합니다.
3. 필요한 경우에만 `README.md`, `IMPLEMENTATION_STATUS.md`, 관련 GDD를 추가로 읽습니다.

공유 문서는 탐색의 시작점이지 코드보다 강한 진실의 원천이 아닙니다. 충돌 시에는 최신 사용자 지시, 실행되는 코드와 테스트, 공유 문서, 기타 설명 문서 순으로 판단하고 충돌 사실을 공유 문서에 남깁니다.

## 작업 중 기록 원칙

- 다음 컨텍스트가 재탐색해야 할 정보를 발견하면 `docs/SHARED_CONTEXT.md`를 갱신합니다.
- 반드시 기록할 항목은 확정된 설계 결정, 중요한 인터페이스/경로 변경, 현재 작업 상태, 알려진 문제와 차단 요인, 실제 실행한 검증과 결과입니다.
- 추측은 사실처럼 기록하지 않습니다. 불확실하면 `확인 필요`로 표시하고 확인 방법을 함께 적습니다.
- 테스트를 실행하지 않았다면 통과했다고 기록하지 않습니다.
- 비밀, 토큰, 개인 정보, 로컬 전용 인증 정보는 기록하지 않습니다.
- 단순 포맷 변경이나 사소한 수정처럼 인수인계 가치가 없는 내용은 기록하지 않습니다.

## 작업 종료 시 필수 절차

작업 결과가 다음 컨텍스트의 판단에 영향을 준다면 종료 전에 공유 문서를 갱신합니다.

1. `현재 작업`을 완료/중단 상태에 맞게 정리합니다.
2. `최근 인수인계` 맨 위에 날짜, 변경 요약, 검증 결과, 남은 일을 짧게 추가합니다.
3. 오래되어 무효가 된 항목은 삭제하거나 `대체됨`으로 명확히 표시합니다.
4. 최근 인수인계는 최신 10개만 유지하고, 영구적으로 필요한 내용은 해당 상위 섹션으로 옮깁니다.

문서만 읽거나 아무 변경 없이 조사한 작업은 새로운 사실이 생긴 경우에만 갱신합니다.

## 모바일·배포 구조 안전 규칙

아래 규칙은 Android와 PC 사이의 실행 차이를 막기 위한 저장소 전체 강제 규칙입니다. 상세 판정표와 예외 절차는 [`docs/MOBILE_RUNTIME_SAFETY_RULES.md`](docs/MOBILE_RUNTIME_SAFETY_RULES.md)를 따릅니다.

### 내보낸 리소스

- 런타임 플레이 가능성 판정에서 `res://`를 `ProjectSettings.globalize_path()`로 OS 경로화하거나 `FileAccess`/`DirAccess`의 물리 디렉터리 존재 여부에 의존하지 않습니다.
- 내보낸 리소스의 존재·로드 여부는 `ResourceLoader.exists()`, `load()` 또는 이미 역직렬화된 Resource 필드로 판정합니다.
- 원본 폴더 구조 검사는 편집기·저작·빌드 감사 함수로 분리하고 런타임 초기화나 프로필 유효성 판정에 섞지 않습니다. `user://` 저장 파일의 원자 교체를 위한 OS 경로 사용은 예외입니다.
- 필수 데이터가 없으면 구형 UI나 기본값으로 조용히 진행하지 않습니다. 관련 행동을 비활성화하고 안정 오류 코드, 실제 원인, `BuildInfo` 식별자를 화면과 로그에 남깁니다.

### 터치·포커스·스크롤

- 터치 대상 페이지 전환에서 `grab_focus()`를 무조건 호출하지 않습니다. 키보드/게임패드 입력에서만 포커스를 복원하고 터치/마우스 입력 뒤에는 잔류 포커스를 해제합니다.
- `ScrollContainer` 안의 비상호작용 Control은 스크롤 제스처를 중단하지 않아야 합니다. `MOUSE_FILTER_STOP`은 실제 탭/클릭 동작을 소유한 컨트롤에만 사용하고 이유를 테스트로 고정합니다.
- 스크롤 완료 판정은 `scroll_vertical` 값을 직접 대입하는 테스트로 대체하지 않습니다. 실제 press/motion/release 입력을 보내 카드의 서로 다른 위치에서 스크롤 값이 변하는지 검사합니다.
- 포인터 모드와 방향키 모드를 둘 다 검증합니다. 포인터 모드에는 상시 focus 표시가 없어야 하고 방향키 모드에는 첫 포커스·순환·복귀 포커스가 있어야 합니다.

### 장면 전환과 폴백

- `change_scene_to_file()`의 반환 오류를 무시하지 않습니다. 장면 내부의 시작 계획 실패와 장면 전환 실패를 서로 다른 오류 코드로 기록합니다.
- 호환 폴백은 선택 기능에만 허용합니다. 필수 콘셉트·스테이지·씬 누락을 정상 화면처럼 보이는 레거시 흐름으로 숨기지 않습니다.
- 메뉴 → 설정 → 게임처럼 여러 계층을 통과하는 변경은 최종 목적지 장면에 머무는지 블랙박스 테스트를 추가합니다.

### 검증과 배포 완료 조건

- 리소스 경로, Autoload 초기화, 메뉴/입력, 장면 전환, export 설정 중 하나라도 변경하면 `tools/run_mobile_release_quality_gate.ps1`를 실행합니다.
- 이 게이트는 기존 헤드리스 7단계, 실제 창 touch/focus, 저장소와 무관한 빈 작업 디렉터리의 Android packed-export 검사를 모두 통과해야 합니다. 저장소 루트나 압축 해제 APK 폴더에서만 실행한 결과는 packed-export 검증으로 인정하지 않습니다.
- Android APK는 원칙적으로 `tools/build_android_debug.ps1`로 생성하고, 파일명·`application/config/version`·`version/name`·`version/code`·내부 build ID·옆 manifest를 대조합니다.
- 자동 게이트 통과와 실기기 승인을 구분합니다. 신규 설치·기존 데이터 유지 업그레이드·터치·중단/복귀·safe area·logcat·10분 성능을 확인하지 않았다면 “실기기 승인” 또는 “모바일 완료”로 기록하지 않습니다.
- 테스트하지 않은 항목은 통과로 기록하지 않으며, 차단된 단계와 재현 명령을 `docs/SHARED_CONTEXT.md`에 남깁니다.
